---
"mattpocock-skills": minor
---

Add `agent-safety`, a model-invoked engineering skill that installs cross-harness guardrails: Cursor `.cursor/hooks.json` (Grok and cloud agents), Claude Code PreToolUse, and an AGENTS.md prompt rail for Codex. The classifier is fail-closed and treats `git -C . push` as a push.
